spring:
  datasource:
    url: jdbc:postgresql://localhost:5451/account_test_db
    username: account
    password: password

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml